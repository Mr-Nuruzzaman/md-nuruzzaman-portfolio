import { CaseStudySkeleton } from '@/components/skeletons/CaseStudySkeleton';

/** Case-study route loading state — shimmer skeleton of the article layout. */
export default function Loading() {
  return <CaseStudySkeleton />;
}
